import type { Metadata } from 'next'
import { Geist, Geist_Mono } from 'next/font/google'

import './globals.css'

const _geist = Geist({ subsets: ['latin'] })
const _geistMono = Geist_Mono({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'AUMC Project Ideas Portal | Air University Multan Campus',
  description: 'Discover and validate innovative project ideas from faculty supervisors at Air University Multan Campus. Browse by department and connect with academic professionals.',
  generator: 'v0.app',
  viewport: {
    width: 'device-width',
    initialScale: 1,
    maximumScale: 1,
    userScalable: false,
  },
}

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode
}>) {
  return (
    <html lang="en" suppressHydrationWarning>
      <body className="font-sans antialiased bg-gradient-to-b from-amber-50 to-white text-amber-950 scroll-smooth">{children}</body>
    </html>
  )
}
